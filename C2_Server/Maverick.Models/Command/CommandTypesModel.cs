namespace Maverick.Models.Command
{
    public enum CommandTypes
    {
        INFOCLIENT,
        DOCOMMAND,
        GETFILE,
        SENDFILE,
        RECONNECT,
        REBOOT,
        KILLAPPLICATION,
    }
}
